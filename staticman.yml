aprendizados:
  allowedFields: ["aprendizado", "descricaolonga", "categoria", "ano", "mesNome","mesNumero", "dia"]
  branch: "master"
  filename: "{@timestamp}"
  format: "json"
  moderation: true
  path: "_data/{options.ano}/{options.mesNome}/{options.dia}"
  requiredFields: ["aprendizado"]
