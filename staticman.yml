aprendizados:
  allowedFields: ["aprendizado", "descricaolonga", "categoria", "ano", "mes", "dia"]
  branch: "master"
  filename: "{options.dia}-{@timestamp}"
  format: "json"
  moderation: true
  path: "_data/{options.ano}/{options.mes}"
  requiredFields: ["aprendizado"]
