aprendizados:
  allowedFields: ["aprendizado"]
  branch: "master"
  filename: "aprendizado-{options.dia}-{@timestamp}"
  format: "json"
  moderation: true
  path: "_data/{options.ano}/{options.mes}"
  requiredFields: ["aprendizado"]
