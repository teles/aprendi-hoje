aprendizados:
  allowedFields: ["aprendizado", "descricaolonga", "categoria", "ano", "mes", "dia"]
  branch: "master"
  filename: "{@timestamp}"
  format: "json"
  moderation: true
  path: "_data/{options.ano}/{options.mes}/{options.dia}"
  requiredFields: ["aprendizado"]
