aprendizados:
  allowedFields: ["aprendizado", "descricaolonga", "categoria", "ano", "mesNome","mesNumero", "dia"]
  branch: "master"
  filename: "{@timestamp}"
  format: "json"
  moderation: true
  path: "_data/{fields.ano}/{fields.mesNome}/{fields.dia}"
  requiredFields: ["aprendizado"]
